# Optional. Defaults are suitable for local use.
# This file is a reference only; export overrides in the shell before starting SymType.
# Loopback only; non-local bindings are rejected.
SYMTYPE_HOST=127.0.0.1
SYMTYPE_PORT=4173
# SYMTYPE_DATA_DIR=/absolute/path/to/private/local/data
# SYMTYPE_OPEN_BROWSER=1
